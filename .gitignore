output.txt
node_modules
dist
.mastra
.env
.env.*
!.env.example
mastra.duckdb*
*.db
*.db-*
.netlify
.vercel
# Claude Code: the skills in .claude/skills/ and the path-scoped rules in
# .claude/rules/ are vendored and shared with every clone; personal settings
# (settings.local.json) and machine state are not.
.claude/*
!.claude/skills/
!.claude/rules/

# Beads / Dolt files (added by bd init)
.dolt/
.beads-credential-key
.beads/
.beads.gate.lock

# Author-only build artifacts (specs, progress, build prompts)
SPEC/
prompts/
.mastracode/

# Built simulation config — authored.yaml and risks.yaml are the sources
simulations/scenarios.yaml
